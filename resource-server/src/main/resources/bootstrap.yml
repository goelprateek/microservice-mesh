spring:
  application:
    name: resource-server
  cloud:
    config:
      uri: ${CONFIG_SERVER_URL:http://localhost:9094}
      fail-fast: true
    bus:
      enabled: true
      trace:
        enabled: true
